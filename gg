Parse.Cloud.define("calcularFatorial", async (request) => {
  const { n } = request.params;
  return { resultado: fatorial(n) };
});

function fatorial(n) {
  return n === 0 ? 1 : n * fatorial(n - 1);
}
